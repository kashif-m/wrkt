[package]
name = "tracker_ffi"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tracker_ffi_core = { path = "../tracker_ffi_core" }

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]
