[package]
name = "tracker_planning"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tracker_ir = { path = "../tracker_ir" }
tracker_engine = { path = "../tracker_engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"