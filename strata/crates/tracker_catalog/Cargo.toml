[package]
name = "tracker_catalog"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tracker_ir = { path = "../tracker_ir" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
