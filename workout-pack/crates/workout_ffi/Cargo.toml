[package]
name = "workout_ffi"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde_json = "1"
tracker_ffi_core = { path = "../../../strata/crates/tracker_ffi_core" }
workout_pack = { path = "../.." }

[lib]
name = "tracker_ffi"
crate-type = ["rlib", "staticlib", "cdylib"]
